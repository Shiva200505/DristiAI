# Benchmarking

`python benchmarks/run_benchmarks.py --component scanner --iterations 10` measures the local scanner and writes a structured JSON result labelled with the development machine. It does not claim Snapdragon performance. Imported remote results must carry device, chipset, model, runtime, precision, job/source, timestamp, latency, throughput, and memory evidence before being shown as a comparison.
