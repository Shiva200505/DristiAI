fastapi>=0.100
uvicorn>=0.23
sqlalchemy>=2.0
pydantic>=2.0
psutil>=5.9
# Optional local security/model adapters:
# bandit>=1.7
# semgrep>=1.90
# chromadb>=0.5
# llama-cpp-python>=0.3
# sentence-transformers>=3.0  # install with a locally cached MiniLM model for semantic retrieval
# onnxruntime>=1.18         # CPU execution; use a Qualcomm/QNN build only on a supported target
